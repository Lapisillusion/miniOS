# GRUB 2 configuration for miniOS (Multiboot2 boot method)
# This file is copied into isodir/boot/grub/ for ISO creation.

set timeout=0
set default=0

menuentry "miniOS" {
    multiboot2 /boot/kernel.elf
    boot
}
